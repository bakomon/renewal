# Example environment variables for renewal scripts
# Copy this file to .env and fill in your values (do NOT commit secrets).
# Add .env to .gitignore.

# Optional: path to local Chrome/Chromium binary (Windows example)
# Use this when you've skipped Puppeteer's Chromium download and want to run with a local Chrome.
CHROME_PATH="C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe"

# Note: to skip downloading Chromium during npm install when Chrome is installed locally, run:
#   macOS / Linux:  PUPPETEER_SKIP_DOWNLOAD=true npm install
#   PowerShell:     $env:PUPPETEER_SKIP_DOWNLOAD="true"; npm install
#   cmd.exe:        set PUPPETEER_SKIP_DOWNLOAD=true && npm install

# Repository / gist (optional, used by deploy / backup scripts)
GH_REPO=owner/repo
GIST_ID=

# Optional proxy settings (set PROXY_URL to include protocol and host:port)
# Example: http://proxy.example.com:3128 or socks5://127.0.0.1:1080
PROXY_URL=
PROXY_USERNAME=
PROXY_PASSWORD=

# Common credentials (replace with provider-specific names as needed)
EMAIL=you@example.com

# Zampto (example provider-specific variables)
ZAMPTO_PASSWORD=
ZAMPTO_SERVER=

# HideNCloud provider
HIDENCLOUD_PASSWORD=
HIDENCLOUD_SERVER=

# WebHostMost provider
WEBHOSTMOST_PASSWORD=

# Add other provider variables below, e.g.:
# PROVIDER1_EMAIL=
# PROVIDER1_PASSWORD=
# PROVIDER1_SERVER=
